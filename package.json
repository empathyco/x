{
  "name": "x",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "lerna bootstrap --hoist",
    "version": "node scripts/create-release-branch.js release && npm run lerna:version",
    "version:alpha": "node scripts/create-release-branch.js release && npm run lerna:version -- --no-push",
    "lerna:version": "lerna version --conventional-commits --no-push",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish": "lerna publish from-package --yes"
  },
  "devDependencies": {
    "lerna": "~4.0.0"
  }
}
