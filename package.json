{
  "name": "x",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "~4.0.0"
  }
}
