{
  "name": "x",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "prepare-release:stable": "node scripts/prepare-release",
    "prepare-release:alpha": "node scripts/prepare-release alpha",
    "publish-release": "node scripts/publish-release"
  },
  "devDependencies": {
    "lerna": "~4.0.0"
  }
}
