{
  "name": "x",
  "license": "Apache-2.0",
  "scripts": {
    "postinstall": "lerna bootstrap --hoist",
    "version": "lerna version --conventional-commits --no-push",
    "version:alpha": "lerna version --conventional-commits --conventional-prerelease --no-push",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish": "lerna publish from-package --yes"
  },
  "devDependencies": {
    "lerna": "~4.0.0"
  }
}
