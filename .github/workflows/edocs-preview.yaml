name: Create eDocs preview
on:
  pull_request:
    paths:
      - packages/x-components/static-docs/*
env:
  GH_TOKEN: ${{ secrets.SUPPORT_TOKEN }}
jobs:
  preview-edocs:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger eDocs reusable workflow
        run: |
          curl --request POST \
          -u empathyco:${{env.GH_TOKEN}} \
          --header 'content-type: application/json' \
          --url https://api.github.com/repos/empathyco/platform-reusable-github-actions/actions/workflows/40476242/dispatches \
          --data '{"ref": "main",  "inputs": {"teamRepo": "x", "branch": "${{ github.head_ref }}", "prNum": "${{ github.event.pull_request.number }}", "prMessage": "⚠️ Only for Empathy members ⚠️ Sorry if there is any inconvenience 😉 \n"}}'