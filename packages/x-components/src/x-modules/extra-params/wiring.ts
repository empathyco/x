import { createWiring } from '../../wiring/wiring.utils';

/**
 * Wiring configuration for the {@link ExtraParamsXModule | extra params module}.
 *
 * @internal
 */
export const extraParamsWiring = createWiring({});
