import { createWiring } from '../../wiring'

/** Wiring configuration for the {@link AiXModule | AI module}. */
export const aiWiring = createWiring({})

// TODO - use-question.vue composable from motiveMarket
